// Install deb

usermod -aG sudo user
nano /etc/sudoers
sudo usermod -L root

sudo apt update
sudo apt upgrade

#ThinkPad X220
#sudo apt install fish git firefox qbittorrent sway swaylock foot slurp grim imv mpv wl-clipboard brightnessctl pipewire pipewire-pulse wireplumber pipewire-alsa #pamixer pavucontrol  ufw linux-headers-amd64 firmware-linux firmware-misc-nonfree polkitd kanshi unzip zip lm-sensors tlp tlp-rdw tp-smapi-dkms #acpi-call-dkms nmap tcpdump libglib2.0-bin xdg-desktop-portal-wlr curl neovim build-essential ripgrep fd-find python3-pip python3-venv htop #fastfetch

#ThinkPad T480S
sudo apt install fish git firefox sway swaylock foot slurp grim imv wl-clipboard brightnessctl pipewire pipewire-pulse wireplumber pipewire-alsa pamixer pavucontrol ufw intel-microcode linux-headers-amd64 firmware-linux firmware-misc-nonfree polkitd kanshi unzip zip lm-sensors tlp tlp-rdw nmap tcpdump libglib2.0-bin xdg-desktop-portal-wlr curl neovim build-essential ripgrep fd-find python3-pip python3-venv htop fastfetch


sudo mkdir /boot/grub/themes
sudo nvim /etc/default/grub
GRUB_THEME="/boot/grub/themes/thinkpad/theme.txt"
sudo update-grub

sudo nvim /etc/apt/sources.list
deb http://deb.debian.org/debian/ stable main non-free non-free-firmware contrib
deb http://security.debian.org/debian-security stable-security main non-free non-free-firmware contrib
deb http://deb.debian.org/debian/ stable-updates main non-free non-free-firmware contrib
sudo apt update

systemctl enable --now ufw
sudo ufw default deny incoming
sudo ufw default allow outgoing
sudo ufw enable
sudo ufw status verbose

systemctl --user --now enable pipewire.socket pipewire-pulse.socket wireplumber.service

sudo sensors-detect


fish_config
echo 'set -g fish_greeting ""' > ~/.config/fish/config.fish

mkdir -p ~/.config/nvim
nvim ~/.config/nvim/init.lua

mkdir ~/.config/kanshi
nvim ~/.config/kanshi/config

mkdir -p ~/.config/fastfetch
nvim ~/.config/fastfetch/config.jsonc

sudo nvim /etc/tlp.conf
sudo tlp start

mkdir ~/.themes
mkdir ~/.icons

mkdir -p ~/.config/sway
mkdir -p ~/.config/foot
nvim ~/.config/sway/bar.sh
chmod +x ~/.config/sway/bar.sh

cp /etc/sway/config ~/.config/sway/config
cp /etc/xdg/foot/foot.ini ~/.config/foot/foot.ini

mkdir -p ~/Pictures/Wallpapers
mkdir -p ~/Pictures/Screenshots
